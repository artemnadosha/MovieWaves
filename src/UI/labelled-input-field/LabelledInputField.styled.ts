import styled from "styled-components";

export const LabelledInputFieldWrapper = styled.label(({}) => ({
  userSelect: "none",
  display: "flex",
  alignItems: "center",
  gap: "8px",
  cursor: "pointer",
}));
